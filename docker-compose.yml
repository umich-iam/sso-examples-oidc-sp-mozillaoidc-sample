version: '3.7'

services:

  app:
    build:
      context: ./src
    env_file: ./src/.env
    volumes:
      - ./src:/usr/src/app
    command: ['python3', 'manage.py', 'runserver', '0.0.0.0:8000']
    ports:
      - 8000:8000
